import 'package:flutter/material.dart';

class AppColors {
  // Primary Palette (Neon Green)
  static const Color primary = Color(0xFF0DF20D); // #0df20d
  static const Color primaryDark = Color(0xFF00CC76);
  static const Color primaryLight = Color(0xFF33FF33);

  // Backgrounds (Dark Forest / Charcoal)
  static const Color background = Color(0xFF102210); // #102210 (Main Hub)
  static const Color backgroundDark = Color(0xFF050505); // #050505 (Focus Mode)

  // Surfaces
  static const Color surface = Color(0xFF182818); // #182818
  static const Color surfaceDark = Color(0xFF121212);
  static const Color surfaceHighlight = Color(0xFF1E3A1E); // #1e3a1e

  static const Color secondary = Color(
    0xFF2DD4BF,
  ); // Teal from Settings Page design

  // Accent Colors
  static const Color accentBlue = Color(0xFF60A5FA); // Blue-400
  static const Color accentYellow = Color(0xFFEAB308); // Yellow-500
  static const Color accentRed = Color(0xFFEF4444); // Red-500

  // Text Colors
  static const Color textPrimary = Color(0xFFFFFFFF);
  static const Color textSecondary = Color(0xFF9CA3AF); // Gray-400
  static const Color textSubtle = Color(0xFF6B7280); // Gray-500

  // Status Colors
  static const Color success = Color(0xFF0DF20D);
  static const Color error = Color(0xFFEF4444);
  static const Color warning = Color(0xFFF59E0B);
  static const Color info = Color(0xFF3B82F6);
}
